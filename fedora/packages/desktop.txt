# Fedora desktop packages for Omarchy Quattro (Hyprland + Omarchy shell)
#
# NOTE: entries that must come from COPR are split out into
# fedora/packages/install -- see fedora/scripts/lib/deps.sh which installs
# COPR repos first, then installs these via dnf.
#
# The Hyprland desktop core:
hyprland
quickshell
hyprpicker
hyprsunset
hyprland-guiutils
# xkbcli (from libxkbcommon-utils) resolves code: keys in omarchy-menu-keybindings
libxkbcommon-utils
# uwsm from nett00n/hyprland only requires /usr/bin/python3, but imports the
# xdg module at runtime (xdg.BaseDirectory etc.); pyxdg is its missing dep.
uwsm
python3-pyxdg
xdg-desktop-portal-hyprland
gtk4-layer-shell
sddm
qt6-qtwayland
qt6-qtimageformats
gpu-screen-recorder

# IME
fcitx5
fcitx5-gtk
fcitx5-qt
